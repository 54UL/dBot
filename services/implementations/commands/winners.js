const { SlashCommandBuilder } = require('@discordjs/builders');

module.exports = {
    data: new SlashCommandBuilder()
        .setName('winners')
        .setDescription('💎💎💎 Diamond hands 💎💎💎'),
    async execute(interaction,dependecy) {
        



        
    }
};
